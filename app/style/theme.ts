import { DefaultTheme, Colors } from 'react-native-paper';

const Theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: Colors.blue700,
    accent: "#FFF",
    background: "#1C1C1E",
    text: Colors.white,
  },

};

export default Theme;
