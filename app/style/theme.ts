import { DefaultTheme, Colors } from 'react-native-paper';

const Theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: "#2DD54B",
    accent: "#FFF",
    background: "#FAFAFA",
    text: Colors.grey900,
  },

};

export default Theme;
